export interface Guest {
    confirm: boolean;
    guests: number;
    id: number;
    name: string;
    phone: number;
  }

  export type monthsTranslations = {
    en:string;
    es:string 
  }
  
  